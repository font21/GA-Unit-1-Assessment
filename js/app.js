// Message if scripts are connected and running
	console.log('JavaScript connected.');
	
// jQuery Resursive Wrap
$(() => {
	console.log('jQuery connected.');
	
// Starting variables
	let TheDisplayedNumber = 0;
	let incrementVar = 0;

// Functions
	incrementorator = (doToVar, whatDoVar) => {
		doToVar = TheDisplayedNumber doToVar whatDoVar;
		upperText = TheDisplayedNumber;
		return
	}

	incrementationDisplay = () => {
		
	}
// Setup Page
	upperText = $('.upper');
	$upperText.text('Incrementorator');


// Input text Box

// Buttons
	on(press) = {
		incrementorator( TheDisplayedNumber, incrementVar );
	}



// jQuery Resursive Wrap Close
})